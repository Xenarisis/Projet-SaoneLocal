server {
    listen 80;
    server_name saone-local.ddns.net;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name saone-local.ddns.net;

    ssl_certificate /etc/letsencrypt/live/saone-local.ddns.net/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/saone-local.ddns.net/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /var/www/html/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ ^/storage/.*\.(php|pl|py|cgi|twig|sh|bat)$ {
        return 403;
    }

    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}